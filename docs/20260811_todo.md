# 2026-08-11 开发 TODO

本文只维护当前尚未完成、尚未验收或需要明确拒绝的能力。已经完成的实现与事故经过分别见正式架构文档和
`development_log.md`，不再把已完成阶段堆进 TODO。

## 当前可用基线

- SFT：fixed padded、DDP/FSDP/DeepSpeed、bounded planned batching、单图 greedy-varlen packing、
  checkpoint/resume 与 HF/PEFT export 已有对应门禁。
- OPD：HF/vLLM rollout、本地/HTTP teacher、full/chunk/top-k-tail objective、专用 telemetry，以及
  DDP/FSDP/DeepSpeed 已进入能力矩阵；准确边界见 `architecture.md` 的 OPD 执行合同。
- RL：DPO、PPO、GRPO 仍按各自成熟度管理，不能用“TRL 能启动”替代 Shaft 的训练正确性验收。

## P0：消除错误能力声明

### 1. DPO/GRPO 的 FSDP + PEFT exact resume

状态：未完成。

- 在共享 restore contract 完成前，配置必须 fail closed。
- 实现后分别验证 DPO、GRPO 的两 rank fresh/resume、标准 adapter、optimizer、scheduler、RNG、Trainer state
  与损坏 artifact 负例。
- 不允许直接复用 SFT 的成功结论，也不在各 RL trainer 复制一套 adapter preload。

### 2. PPO 训练正确性

状态：仅 debug smoke，不是生产能力。

- value/critic role 必须在 optimizer plan 前完成装配，证明全部预期参数获得梯度并更新。
- Shaft sample plan 必须是唯一数据顺序真源，禁止上游 DataLoader 再次 shuffle。
- 接入具有不可变身份和加载校验的真实 reward model；`allow_untrained_reward_model` 只能保留为显式 debug
  开关，不能成为生产配置。
- 当前 text-only rollout 必须保持明确边界；若支持多模态 PPO，需要补齐 image 输入到 policy/value/reward 的
  有序媒体合同。`allow_text_only_multimodal_ppo` 不能被视为多模态支持。
- full-parameter PPO、真实 RM、KL/reward/entropy/ratio telemetry 与异常阈值都需要独立 release gate。
- 补 checkpoint/resume、最终 artifact reload 与真实多卡最小门禁；完成前文档和配置继续标记受限。

### 3. Qwen3.6 MTP artifact 一致性

状态：未实现。

- 模型 config 声明 MTP 时，checkpoint tensor 必须区分 `absent / inherited / trained`；缺失或残缺权重不能
  静默导出为 enabled artifact。
- 若实现可训练 MTP，objective 必须由 Qwen3.6 model policy 持有，并验证 next-N shift、监督 mask、保存恢复
  与 vLLM speculative decode；不能把模型专属逻辑放进通用 trainer/collator。

## P1：训练域生产化

### 4. DPO release gate

- 真实 Qwen 单卡与 DDP forward/backward、fresh/resume、HF/PEFT export/reload。
- `precompute_ref_log_probs`、`label_smoothing` 和未消费参数必须实现真实语义或提前拒绝。
- FSDP/DeepSpeed、planned batching、packing/varlen 只有专项门禁后才开放。

### 5. GRPO release gate

- 让 `data.max_length`、rollout cadence、grouped sampler 与 checkpoint safe boundary 使用同一合同。
- 完成真实 Qwen、DDP、final export/reload 和 online eval 验收。
- GRPO vLLM 的 server RNG/恢复状态未进入 checkpoint 前，继续禁止 periodic save/resume。
- 删除或正式接入未被主链消费的输入实现，避免第二套 collator contract。

### 6. OPD 后续专项

以下不是当前 OPD 主链 blocker，但不能从现有单图/tiny 证据外推：

- 发布权重的有序多图 vLLM rollout。
- 独立 Qwen HTTP teacher GPU 部署与吞吐/限流测试。
- 发布 Qwen 的 FSDP/DeepSpeed OPD 容量门禁。
- 大词表 full/chunk/top-k-tail 的显存和吞吐 A/B。
- 跨 step rollout buffer、PG-OPD/OPSD；引入 buffer 前必须先设计持久化 cursor/state。

## P2：高级 batching 与并行

### 7. Sharded planned batching 扩展

当前 FSDP/DeepSpeed planned SFT 只开放 bounded-cost fixed padded。待开发：

- token-budget cardinality。
- length grouping 与 greedy-varlen packing。
- world-size elastic resume。
- DPO/GRPO/PPO planned batching。

每个组合都必须验证 draw 守恒、无二次分片、GA boundary、backend-native resume 和 GPU canary，不能只解除
normalize 限制。

### 8. 多图 sequence packing

- padded SFT/DPO 与有序多图推理已支持；varlen/greedy packing 仍只支持单图。
- 需要统一 media-segment 顺序、attention isolation、M-RoPE/position、pixel/grid slice，并补模型族 GPU
  correctness；不能在通用 varlen builder 中硬编码 Qwen 字段。

### 9. 分布式与编译

- 真实双主机 NCCL/NIC/共享存储 canary。
- tensor/context/sequence parallel。
- `torch.compile` 支持矩阵。
- 后端 checkpoint 的可扩展内容身份，避免为数百 GiB shard 增加第二次全量读取。

### 10. 发布规模验收

- Qwen3.5/3.6 MoE 35B 真实权重。
- Qwen3VL dense 32B 与 30B-A3B 的长程收敛、吞吐和 full-parameter 容量。
- varlen FP16、FSDP FP16、adapter-on-adapter provenance。
- PEFT `target_parameters` 与 freeze override 的长期稳定性。

## 推荐顺序

1. 先让 DPO/GRPO FSDP+PEFT 未验证组合明确 fail closed。
2. 修复 PPO optimizer/data-order correctness，或继续保持 debug-only。
3. 完成 DPO release gate，再收口 GRPO。
4. 按真实训练需求选择 OPD 多图/部署、sharded batching 或 MTP，不并行扩张全部矩阵。
5. 最后做多机、并行与发布规模容量验收。
