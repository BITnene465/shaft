"""Adapters for external runtimes such as Shaft.

Adapters should keep the prediction snapshot boundary as the only required
output of inference. Shared Shaft-side visualization helpers live under
``shaft.metrics``.
"""
