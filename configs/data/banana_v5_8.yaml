# Banana v5.8 preparation catalog.
#
# Physical train sources are owned by
# data.prompt_sources.<dataset>.formulation_sources. This catalog remains preparation-only until
# the builders and data audits complete.
datasets:
  grounding_layout:
    source_type: jsonl_sft
    weight: 8.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / real layout grounding formulations
    tags: [banana, v5.8, preparation, grounding, layout, real, formulations, sft]

  shape_context_reconstruction:
    source_type: jsonl_sft
    weight: 5.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / V9 shape type, geometry, and reconstruction
    tags: [banana, v5.8, preparation, shape, synthetic, v9, formulations, sft]

  line_context_reconstruction:
    source_type: jsonl_sft
    weight: 6.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / V9 line topology, points, and reconstruction
    tags: [banana, v5.8, preparation, line, synthetic, v9, formulations, sft]

  line_context_points:
    source_type: jsonl_sft
    weight: 2.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / line reconstruction points-only eligibility pool
    tags: [banana, v5.8, preparation, line, reconstruction, points_only, real, synthetic, formulations, sft]

  image_context_reconstruction:
    source_type: jsonl_sft
    weight: 1.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / reviewed real 13-class image type
    tags: [banana, v5.8, preparation, image, classification, real, formulations, sft]
