# Banana v5.8 preparation catalog.
#
# Grounding and background keep their ordinary materialized train/val sources here.
# Multi-formulation task sources are owned by
# data.prompt_sources.<dataset>.formulation_sources. All sources referenced here are materialized
# and audited; the paired train recipe keeps its preparation suffix until a training canary runs.
datasets:
  grounding_layout:
    source_type: jsonl_sft
    train_path: ../../data/grounding_layout/sft/train.jsonl
    val_path: ../../data/grounding_layout/sft/val.jsonl
    weight: 8.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / real unified layout grounding
    tags: [banana, v5.8, preparation, grounding, layout, real, sft]

  background:
    source_type: jsonl_sft
    train_path: ../../data/background/sft/train.jsonl
    val_path: ../../data/background/sft/val.jsonl
    weight: 1.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / reviewed full-image background classification
    tags: [banana, v5.8, preparation, background, reconstruction, real, reviewed, sft]

  shape_context_reconstruction:
    source_type: jsonl_sft
    weight: 5.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / V9 shape appearance, geometry, and reconstruction
    tags: [banana, v5.8, preparation, shape, synthetic, v9, formulations, sft]

  line_context_reconstruction:
    source_type: jsonl_sft
    weight: 6.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / V9 line appearance, points, and reconstruction
    tags: [banana, v5.8, preparation, line, synthetic, v9, formulations, sft]

  line_context_points:
    source_type: jsonl_sft
    weight: 2.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / points-only cohort using the shared line reconstruction pool
    tags: [banana, v5.8, preparation, line, reconstruction, points_only, real, synthetic, formulations, sft]

  image_context_reconstruction:
    source_type: jsonl_sft
    weight: 1.0
    enabled: true
    use_for_eval: false
    help: banana v5.8 preparation / reviewed real 13-class image type
    tags: [banana, v5.8, preparation, image, classification, real, formulations, sft]
