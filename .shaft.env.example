# scripts/train.py loads this project-local file before importing the training stack.
# Copy it to .shaft.env and point CUDA_HOME at the toolkit root containing bin/nvcc.
CUDA_HOME=/path/to/cuda-toolkit
